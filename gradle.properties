org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# O token da Bluesoft Cosmos (COSMOS_API_TOKEN e COSMOS_USER_AGENT) NÃO vai aqui —
# este arquivo é versionado no Git. Configure-os no local.properties (que já está
# no .gitignore) ou como variável de ambiente. Veja o README para instruções.
