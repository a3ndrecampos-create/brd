name: Build APK

# Gera o APK de debug a cada push/PR na main e disponibiliza como artefato para
# download na aba "Actions" do GitHub (Summary da execução > Artifacts).
# Não precisa de nenhuma chave/segredo: a Open Beauty Facts é pública.
on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Cache do Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Dar permissão de execução ao gradlew
        run: chmod +x ./gradlew

      - name: Build do APK de debug
        run: ./gradlew assembleDebug --stacktrace

      - name: Publicar o APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: beautymanager-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
